# 安全说明

额度助手是本地桌面工具，只使用用户已经授权的 Codex 登录状态，以及用户在独立 Claude 登录窗口中建立的会话。

## 安全边界

- 不持久化或记录 Codex 访问令牌和 Claude Cookie。
- 不记录请求头、账户 ID 或原始额度响应。
- 认证文件读取上限为 256 KB，接口响应读取上限为 1 MB。
- 额度请求不自动跟随重定向。
- 不兑换重置机会，不修改订阅或账户设置。
- Claude 会话与用户的日常浏览器会话相互隔离。

分享诊断信息、截图或错误报告前，仍应检查并遮盖个人信息。发布安装包前应使用维护者控制的签名证书和更新密钥；未签名的开发构建必须明确标注。
