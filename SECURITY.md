# 安全说明

额度助手是本地桌面工具，只使用用户已经授权的 Codex 登录状态，以及用户在独立 Claude 登录窗口中建立的会话。

## 支持版本

| 版本 | 安全更新状态 |
|---|---|
| 最新公开正式版本 | 支持；安全修复只进入新的候选版本并经过独立验收后发布 |
| 更早版本和未发布构建 | 不支持；不得把本地候选包当作正式更新 |

## 私密漏洞报告

当前仓库尚未启用 GitHub Private Vulnerability Reporting，也没有由维护者确认的安全报告邮箱。请不要在公开 Issue、讨论、截图或诊断报告中粘贴令牌、Cookie、密码、验证码、账户 ID 或原始认证文件。

在维护者启用 GitHub 私密漏洞报告或提供经过确认的私密联系方式之前，本候选版本的“私密报告渠道”发布门保持未完成。维护者完成配置后，应把唯一的私密入口写在这里，并在全新未登录浏览器中验证它确实可以提交；不得用普通公开 Issue 代替。

## 安全边界

- 不持久化或记录 Codex 访问令牌、Claude Cookie、Apple 密码或验证码。
- 不记录请求头、账户 ID 或原始额度响应。
- 本地订阅缓存只包含套餐、来源、周期、续期日期和状态，不包含账号邮箱或认证信息。
- 认证文件读取上限为 256 KB，接口响应读取上限为 1 MB。
- 额度请求不自动跟随重定向。
- 不兑换重置机会，不修改订阅或账户设置。
- Claude 会话与用户的日常浏览器会话相互隔离。
- Apple 登录与订阅查询使用受限的官方页面窗口；用户凭据只提交给 Apple。应用只接收套餐、订阅状态和续期日期等最少必要字段，不接收完整页面正文。

分享诊断信息、截图或错误报告前，仍应检查并遮盖个人信息。GitHub 开源社区版允许提供未签名、未公证的安装包，但必须在下载区、平台表、安装步骤和 Release notes 醒目标注，并提供 SHA-256 校验文件。正式签名发行版仍需要维护者提供受控证书，项目不会自行申请或编造证书。
