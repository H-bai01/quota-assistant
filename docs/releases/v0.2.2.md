# 额度助手 v0.2.2

Release tier: GitHub community

> **Unsigned downloads:** the macOS package is not Developer ID signed or notarized, and the Windows package is not Authenticode signed. Download only from `H-bai01/quota-assistant` Releases and verify every package with `SHA256SUMS.txt` before running it.

本文件是未发布候选说明。平台实机证据、候选 run ID、最终提交和回退结论补齐前不得用于创建正式 Release。

## Changes

- 统一候选版本为 v0.2.2，避免与已公开 v0.2.1 形成同版本不同代码。
- 重写中英文 README，新增真实候选界面、直接下载入口、平台/签名状态、首次使用、故障排查及 SHA-256 验证说明。
- 新增 GitHub 开源社区版发行等级：允许充分披露的未签名包；未来签名发行版仍严格要求平台签名与公证。
- 保持凭据隔离、固定官方 origin、缓存恢复、轮询停止、供应链固定和公开文件边界等既有安全门。

## Supported platforms

- macOS Universal：候选附件名为 `quota-assistant_0.2.2_macos_universal.dmg`；未签名、未公证。最终 SHA 对应的实机冷启动、核心流程、退出和卸载证据尚待补齐。
- Windows x64：候选附件名为 `quota-assistant_0.2.2_windows_x64-setup.exe`；未 Authenticode 签名。真实 Windows 安装包验收尚未完成，不得描述为已正式验证。
- 当前自动测试数字：前端 20 项通过，Rust 31 项通过。正式发布前必须在最终候选提交重新执行并更新此处（如数字变化）。

## Installation

1. 从同一 GitHub Release 下载平台安装包和 `SHA256SUMS.txt`。
2. 使用 SHA-256 工具核验安装包；摘要不一致时立即停止。
3. macOS 可能显示 Gatekeeper 警告；Windows 可能显示 SmartScreen/未知发布者警告。仅在确认官方仓库来源和摘要后继续。

macOS：

```bash
shasum -a 256 quota-assistant_0.2.2_macos_universal.dmg
grep quota-assistant_0.2.2_macos_universal.dmg SHA256SUMS.txt
```

Windows PowerShell：

```powershell
Get-FileHash .\quota-assistant_0.2.2_windows_x64-setup.exe -Algorithm SHA256
Get-Content .\SHA256SUMS.txt
```

## Known limitations

- 未签名社区版可能触发 Gatekeeper、SmartScreen 或安全软件误报。
- Windows v0.2.2 缺少真实安装、冷启动、托盘、拖动、锁定、语言、诊断、退出和卸载证据。
- Google Play 与网页直付订阅读取尚未完成真实账号验证。
- 当前未启用安全自动更新通道。

## Upgrade and rollback

- 升级前退出旧版本，再安装 v0.2.2。
- v0.2.1 尚未完成公开下载与实际降级验证，因此本候选暂不能声称存在已验证回退点。
- 发布前如完成 v0.2.1 公共附件、校验和及双平台实际降级复核，应把上一行改为实际结论，并在 `release-gates.json` 记录证据。

## Traceability

- Source commit: `<final 40-character candidate SHA>`
- Candidate workflow run: `<candidate run ID>`
- Platform validation: pending; final records must be in `release-gates.json`
- Checksums: generated automatically as `SHA256SUMS.txt`
- SBOM: `quota-assistant_0.2.2_windows.cdx.json` and `quota-assistant_0.2.2_macos.cdx.json`
